%FST_DGEMM Operator matrix-matrix multiplication
%  FST_DGEMM(C,A,TRA,B,TRB,{ALPHA=1},{BETA=0})
%
%  C = ALPHA*op(A)*op(B) + BETA*C, op(X) == X or X' (dep. on TRX)
%
%  NOTE: Structure codes are ignored.
